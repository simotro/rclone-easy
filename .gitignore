.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Sidecar rclone (scaricato con scripts/fetch-rclone-sidecar.sh, non versionato)
/src-tauri/binaries/

# Note di sviluppo, materiale sorgente e artefatti locali non destinati al repo pubblico
/dev-notes/

# Stato locale di Claude Code (lock di sessione, non destinato al repo pubblico)
/.claude/

# Symlink creati da `nix build`
/result
/result-*

# Anteprime PNG delle icone tray generate da scripts/generate_tray_icons.py,
# solo per ispezione visiva locale — non consumate dal codice Rust
/src-tauri/icons/tray/*.preview.png
